# Default column to write to
DEFAULT_COLUMN = "A"

# Default Excel file name if none is provided
DEFAULT_EXCEL_FILENAME = "file_names.xlsx"

# Default sheet name (only used if none is passed)
DEFAULT_SHEET_NAME = "File Names"

# Default header text for the column 
DEFAULT_HEADER = "PDF Filenames"

# Whether to include hidden files
INCLUDE_HIDDEN_FILES = False

# Whether to inlcude full paths instead of just names
USE_FULL_PATH = False

